# 项目级 AGENTS.md

> 本文件是当前项目的开发约定，**优先级高于全局规范库**。
> 顶部的「全局规则库（vibe-rules）」引用块由 install / update 脚本自动注入和维护，不要手改。
> 默认模式下规则副本就在本项目 `.vibe-rules/` 里、跟着仓库走：队友 clone、云端 agent、CI 都能直接读到。
> 规则库更新后（先在规则库 `git pull`）重跑一次 `scripts/update.sh <本项目>` 即可刷新副本和引用块。

---

## 1. 项目概览

<!-- TODO: 用一两句说清楚这是什么项目、给谁用、核心业务是什么 -->

- **业务一句话**：
- **技术栈**：
- **部署方式**：（本地 / Docker / K8s / Serverless / ...）
- **主要联系人**：（个人项目可留空）

## 2. 目录速览

<!-- TODO: 列一下关键目录和它们的职责，方便 agent 快速定位 -->
```
src/...
```

## 3. 本项目特殊约定

<!-- TODO: 这里写全局规范不能覆盖的、只对本项目成立的规则。例如：
- 数据库迁移必须走 Flyway/Liquibase，不要直接改 entity
- 接口返回格式统一用 R<T> 包装
- 前端不要直接调后端字段名，要经过 dto 转换
- 不要引入新的 ORM / UI 库
-->

-

## 4. 本地开发

<!-- TODO: 怎么起服务、怎么连数据库、跑测试的命令 -->

```bash
# 启动
# 测试
# lint/typecheck
```

## 5. 历史坑（本项目专属）

<!-- TODO: 只在这个项目里踩过的坑。全局坑去规则库 global/anti-patterns.md 记（路径见顶部引用块）。 -->

## 6. 不要做的事

<!-- TODO: 这个项目里明确禁止的。例如：
- 不要改 legacy/ 目录，那是要下线的
- 不要动 auth 模块，有专人维护
- 不要把 .env.example 里的值当真实配置
-->

-
